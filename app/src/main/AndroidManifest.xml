<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tharapk">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_thar_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.explore.ExploreActivity"></activity>
        <activity android:name=".ui.enquiry.EnquiryActivity" />
        <activity android:name=".ui.admin.AdminSignInActivity" />
        <activity android:name=".ui.map.MapActivity" />
        <activity android:name=".ui.sponsors.SponsorsActivity" />
        <activity android:name=".ui.team.TeamActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.notification.NotificationActivity" />
        <activity android:name=".ui.feed.FeedActivity" />
        <activity android:name=".ui.event.EventActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".SignExp" />
        <activity android:name=".NoConnection" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.event.EventDetailActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>